#include<stdio.h>
int main(){
    int i;
    int arr[]={4,6,890,3,5,7,3};
      int n = sizeof(arr)/sizeof(arr[0]);
      int res = arr[0];
      for(i=0;i<n;i++)
      if(res < arr[i])
      res=arr[i];
      printf("array elements is: ");
      for(i=0;i<n;i++){
      printf("%d ",arr[i]);
      }
      printf("\n");
      printf("maximum number in the array: %d",res);
      return 0;
}